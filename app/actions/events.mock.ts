import { fn } from '@storybook/test';

export const getEvents = fn(async () => []).mockName('getEvents');
export const createEvent = fn(async () => {}).mockName('createEvent');
export const updateEvent = fn(async () => {}).mockName('updateEvent');
export const deleteEvent = fn(async () => {}).mockName('deleteEvent');
